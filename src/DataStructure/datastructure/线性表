线性表：n个具有相同特征的数据元素的有限序列。
前驱元素：
若a元素在b元素的前面，则a是b的前驱元素。
后继元素：
若b元素在a元素的后面，则称b为a的后继元素
线性表的特征：数据元素之间具有一对一的逻辑关系。
    1.第一个数据元素没有前驱，这个数据元素称为头节点；
    2.最后一个数据元素没有后继，这个数据元素被称为尾节点。
    3.除头和尾节点外，其他元素有且仅有一个前驱和一个后继。
分类：
数据的存储结构可以是顺序存储也可以是链式存储，根据存储方式的不同，可以分为顺序表和链表
1.顺序表：数据存储地址连续
2.链表：数据存储地址不连续