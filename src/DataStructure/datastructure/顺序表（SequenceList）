1.一般作为容器存储数据，都需要向外部提供遍历的方式。
实现：
    1.让SequenceList实现terable接口，重写iterator方法；
    2.在SequenceList内部提供一个内部类Siterator,实现iterator接口,重写hasNext方法和next方法;

顺序表的容量可变:
    1.添加元素:检查档期那数组能否容纳新的元素,如果不能则扩容(创建一个新数组)成为原数组的长度的两倍.
    2.移除元素:判断剩余移除后的数组是否小于原数组的1/4如果是,创建新数组(原数组的1/4大小)
时间复杂度分析:
    1.get方法:  O(1)
    2.inserts方法:  O(N)
    3.remove方法:  O(N)
    4.在数组扩容的时候需要像系统申请内存(耗时增加)所以插入数据的时间消耗不是线性的.