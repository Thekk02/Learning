1.普通的队列是一种先进先出的数据结构，从队列尾追加，而从队列头删除。
2.有限队列按照其作用不同，可以分为以下两种：
    1.最大优先队列(MaxpriorityQueue)（可以获取并删除队列中的最大值）
    2.最小优先队列（MinpriorityQueue）（可以获取并删除队列中的最小值），
    实现时就把堆的思想反过来，把最小的元素放在索引1处
3.之前我们学过堆，便于删除最大值
4.索引优先队列（）以最小优先队列为例：
    1.给每个元素关联一个整数，创建原数组items存储值，
    2.完成后堆不是有序的，我们可以添加一个辅助数组pq，来存储每个元素在items数组中的索引，pq数组需要堆有序
    3.添加一个辅助数组qp来存储pq数组的逆序，即把pq数组的元素作为索引，索引作为元素，此时的qp数组的索引与items数组一致方便调整items1数组中元素位置。














